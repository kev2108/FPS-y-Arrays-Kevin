using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickupScript : MonoBehaviour
{
    //crear una variable pública que establezca si es alimento o no
    //crear una variable pública que almacene el valor en una cantidad entera

    public bool alimento;
    public int valor;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
